# temperature

To set the app:

 - Git clone git@github.com:AliTopal89/temperature.git

Setup the rails app:

 - bundle install
 - rake db:create
 - rake db:migrate
 - rails s
Open up http://localhost:3000/ to view the app